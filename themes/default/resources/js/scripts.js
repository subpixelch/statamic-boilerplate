/**
 * First we will load all of this project's JavaScript dependencies.
 */
require('./bootstrap');

$(function () {
    // Use bootstrap-select on all selects by default
    $('select').selectpicker();
});